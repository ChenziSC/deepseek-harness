# `minimal` Agent preset：固定 Prompt、双 Tool 的编码 Agent 组合。
#
# persona 是完整 system prompt，因此全局身份、Web 定位、Tool 指引和后续组装 listener
# 都不能再添加 Prompt 文本。此 preset 不生成运行时上下文快照；模型只装配持久 shell
#（POSIX 上为 `bash`，win32 上为 `pwsh`）和 `str_replace_editor`，且不启用上下文压缩。

- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    # 中文译文（仅供学习和维护，不参与运行时）：你是一名乐于助人的软件工程助手。
    # complete 与 includeRuntimeContext: false 使这句成为唯一 system prompt。
    text: You are a helpful software engineer assistant.
    complete: true
    includeRuntimeContext: false

# PTY 注册表是 Agent 拥有的服务，因此位于条目局部 realm。后端仍消费 Host 的 sandbox
# policy 与 subprocess 实现，而 Tool 注册到此 Agent 的 scoped catalog。每个 Host 恰好
# 挂载一套 shell stack：bash stack 在 win32 上关闭，pwsh 对应项在 POSIX 上关闭，与
# one-shot shell 行保持一致。
- id: persistent-shell
  name: cordis:group
  group: true
  isolate:
    terminals: true
  config:
    - id: pty
      name: '@deepseek-ai/dsh-terminal'

    - id: terminal-bash
      name: '@deepseek-ai/dsh-terminal-bash'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000

    - id: persistent-bash
      name: '@deepseek-ai/dsh-tool-bash-persistent'
      disabled: !!js process.platform === 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a bash shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * You do have access to a mirror of common linux and python packages via apt and pip.
          * State is persistent across command calls and discussions with the user.
          * To inspect a particular line range of a file, e.g. lines 10-25, try 'sed -n 10,25p /path/to/the/file'.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'sleep 10 &' or start a server in the background.

    - id: terminal-pwsh
      name: '@deepseek-ai/dsh-terminal-bash'
      disabled: !!js process.platform !== 'win32'
      config:
        shellDialect: pwsh
        timeoutMs: 300000

    - id: persistent-pwsh
      name: '@deepseek-ai/dsh-tool-pwsh-persistent'
      disabled: !!js process.platform !== 'win32'
      config:
        timeoutMs: 300000
        description: |-
          Run commands in a PowerShell shell
          * When invoking this tool, the contents of the "command" parameter does NOT need to be XML-escaped.
          * You don't have access to the internet via this tool.
          * State is persistent across command calls and discussions with the user.
          * Use native Windows paths (C:\...) and $env:NAME variables; this is PowerShell, not bash.
          * Please avoid commands that may produce a very large amount of output.
          * Please run long lived commands in the background, e.g. 'Start-Job' or start a server with Start-Process.

# 裸本地文件系统只在此 preset 中遮蔽 Host 的 sandboxed Provider。编辑器共享该 realm，
# 并要求使用绝对路径。
- id: filesystem
  name: cordis:group
  group: true
  isolate:
    fs: true
  config:
    - id: fs-local
      name: '@deepseek-ai/dsh-fs-local'
      config:
        cwd: !!js process.env.DSH_CWD ?? process.cwd()

    - id: str-replace-editor
      name: '@deepseek-ai/dsh-tool-str-replace-editor'
      config:
        maxOutputChars: 16000
